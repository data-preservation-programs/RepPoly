{
  "name": "@data-preservation-programs/RepPoly",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "release": "rm -rf dist && npm run build && cp package*.json dist && cd dist && npm ci --omit=dev && zip -r index.zip ."
  },
  "license": "(MIT OR Apache-2.0)",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.109",
    "@types/node": "^18.11.9",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "@polybase/auth": "^0.6.2",
    "@polybase/client": "^0.6.2",
    "@polybase/eth": "^0.6.2",
    "dotenv": "^16.3.1",
    "mongodb": "^5.6.0"
  }
}
